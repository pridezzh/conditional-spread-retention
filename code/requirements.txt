# one-step generative-policy discriminant — reproduction dependencies
# install: python -m pip install -r requirements.txt
# all experiments run on CPU; the CUDA build of PyTorch is not required.
#
# versions are those verified to work during development; newer versions usually
# also work, but scipy's spearmanr / mannwhitneyu return type changed around 1.9
# (this repo uses the 1.11+ object form, i.e. the .statistic attribute); older
# versions would require reverting to tuple unpacking.
numpy>=1.26
scipy>=1.11
torch>=2.2          # CPU build is sufficient: pip install torch --index-url https://download.pytorch.org/whl/cpu
matplotlib>=3.8
scikit-learn>=1.4  # only needed for the digits fallback when the MNIST download fails
